import pandas as pd

df = pd.DataFrame({
    'school_code': ['S001','S001','S002','S002'],
    'class': ['A','B','A','B'],
    'age': [15, 14, 16, 15]
})

grouped = df.groupby(['school_code','class'])
print(grouped.size())
